apiVersion: apps/v1
kind: Deployment
metadata:
  name: capstone-deployment
  labels:
    app: capstone
spec:
  replicas: 2
  selector:
    matchLabels:
      app: capstone
  template:
    metadata:
      labels:
        app: capstone
    spec:
      containers:
      - name: capstone
        image: psnx/cloudnd-capstone:44f5e977d1dc3580e1aaa4badfe6d44e59293bd2
        ports:
        - containerPort: 80
      imagePullSecrets:
      - name: dockerhub
---
#kubectl expose deployment capstone-deployment --type=LoadBalancer --name=cloudnd-capstone --port=80 --target-port=80
apiVersion: v1
kind: Service
metadata:
  name: capstone
  labels:
    run: cloudnd-capstone
spec:
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: capstone
  type: LoadBalancer